# Backend base URL for the patient app.
# Replace <YOUR_LAN_IP> with your computer's LAN IP so a phone on the same
# Wi-Fi can reach the dev server (find it with `ipconfig`).
EXPO_PUBLIC_API_URL=http://<YOUR_LAN_IP>:5000/api
